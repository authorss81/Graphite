<?xml version="1.0" encoding="utf-8"?>
<!--
  Android Network Security Configuration — Graphite Notes
  Place this file at: android/app/src/main/res/xml/network_security_config.xml
  Then add to android/app/src/main/AndroidManifest.xml:
    <application android:networkSecurityConfig="@xml/network_security_config" ...>
-->
<network-security-config>

  <!-- Production: only allow HTTPS, no cleartext (HTTP) -->
  <base-config cleartextTrafficPermitted="false">
    <trust-anchors>
      <!-- System-trusted CAs (Google, Amazon, Let's Encrypt etc.) -->
      <certificates src="system" />
    </trust-anchors>
  </base-config>

  <!-- Allow HTTP only to localhost (Vite dev server) -->
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="false">localhost</domain>
    <domain includeSubdomains="false">127.0.0.1</domain>
    <domain includeSubdomains="false">10.0.2.2</domain>
  </domain-config>

  <!-- Supabase: require HTTPS always -->
  <domain-config cleartextTrafficPermitted="false">
    <domain includeSubdomains="true">supabase.co</domain>
    <trust-anchors>
      <certificates src="system" />
    </trust-anchors>
  </domain-config>

</network-security-config>
